name: test

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-18.04
    timeout-minutes: 10

    steps:
      - name: clone googletest
        run: |
          git clone https://github.com/google/googletest.git
      - name: test
        run: |
          make
          make test
